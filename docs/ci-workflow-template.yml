# CI/CD Pipeline Template
#
# Naming convention: scope-testtype-target
#   Service:    service-build, service-unit-tests, service-integration-tests,
#               service-functional-api-tests, service-coverage-report
#   App:        <projectName>-build, <projectName>-unit-tests
#   Repo-wide:  lint, typecheck, openapi-contract-check
#
# Pipeline stages:
#   1. lint + typecheck + openapi-contract-check
#   2. service-unit-tests (with Postgres service container)
#   3. service-integration-tests
#   4. service-functional-api-tests
#   5. service-coverage-report
#   6. <projectName>-unit-tests
#   7. service-build + <projectName>-build
#   8. (Deploy stages — add when ready)
#   9. Post-deploy smoke tests
#   10. Post-deploy browser E2E

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate --schema=packages/core-api/prisma/schema.prisma
      - run: npm run lint

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate --schema=packages/core-api/prisma/schema.prisma
      - run: npm run typecheck

  openapi-contract-check:
    needs: [lint, typecheck]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate --schema=packages/core-api/prisma/schema.prisma
      - run: npm run openapi-contract-check

  service-unit-tests:
    needs: [lint, typecheck]
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: testdb
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb
      JWT_SECRET: test-secret-change-in-production
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate --schema=packages/core-api/prisma/schema.prisma
      - run: npx prisma migrate deploy --schema=packages/core-api/prisma/schema.prisma
      - name: Service unit tests
        run: npm run test:coverage:service:unit
      - uses: actions/upload-artifact@v4
        with:
          name: service-unit-coverage
          path: coverage/service-unit/
          retention-days: 1

  service-integration-tests:
    needs: [lint, typecheck]
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: testdb
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb
      JWT_SECRET: test-secret-change-in-production
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate --schema=packages/core-api/prisma/schema.prisma
      - run: npx prisma migrate deploy --schema=packages/core-api/prisma/schema.prisma
      - name: Service integration tests
        run: npm run test:coverage:service:integration
      - uses: actions/upload-artifact@v4
        with:
          name: service-integration-coverage
          path: coverage/service-integration/
          retention-days: 1

  service-functional-api-tests:
    needs: [lint, typecheck]
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: testuser
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: testdb
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      DATABASE_URL: postgresql://testuser:testpass@localhost:5432/testdb
      JWT_SECRET: test-secret-change-in-production
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate --schema=packages/core-api/prisma/schema.prisma
      - run: npx prisma migrate deploy --schema=packages/core-api/prisma/schema.prisma
      - name: Service functional API tests
        run: npm run test:service:functional-api:coverage
      - uses: actions/upload-artifact@v4
        with:
          name: service-functional-api-coverage
          path: coverage/service-functional-api/
          retention-days: 1

  service-coverage-report:
    needs: [service-unit-tests, service-integration-tests, service-functional-api-tests]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - uses: actions/download-artifact@v4
        with:
          name: service-unit-coverage
          path: coverage/service-unit/
      - uses: actions/download-artifact@v4
        with:
          name: service-integration-coverage
          path: coverage/service-integration/
      - uses: actions/download-artifact@v4
        with:
          name: service-functional-api-coverage
          path: coverage/service-functional-api/
      - name: Merge service coverage
        run: node scripts/merge-service-coverage.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: service-coverage-report
          path: coverage/service-merged/
          retention-days: 7

  projectName-unit-tests:
    needs: [lint, typecheck]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx turbo build --filter=@<projectName>/shared
      - name: "<projectName> unit tests"
        run: npm run "test:<projectName>:unit"

  service-build:
    needs: [lint, typecheck]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npx prisma generate --schema=packages/core-api/prisma/schema.prisma
      - name: Build service
        run: npm run build:service

  projectName-build:
    needs: [lint, typecheck]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: "Build <projectName>"
        run: "npm run build:<projectName>"

  # =========================================================================
  # Deploy stages — uncomment and customize when deployment is ready
  # =========================================================================
  #
  # deploy:
  #   needs: [service-coverage-report, projectName-unit-tests, service-build, projectName-build]
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   runs-on: ubuntu-latest
  #   steps:
  #     - # Build and push Docker image to ECR
  #     - # Register new ECS task definition
  #     - # Run database migration via ECS run-task
  #     - # Run seed via ECS run-task (pass bootstrap credentials via env overrides)
  #     - # Update ECS service
  #     - # Build webapp with release metadata:
  #     #   <PROJECTNAME>_ENVIRONMENT, <PROJECTNAME>_BUILD_TIME_UTC,
  #     #   <PROJECTNAME>_GIT_REF, <PROJECTNAME>_WEBAPP_GIT_SHA
  #     - # Deploy webapp to S3 + invalidate CloudFront
  #     - # Verify version-info.json exists in build output
  #
  # smoke-test:
  #   needs: deploy
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: npm run test:smoke:api

  # =========================================================================
  # Browser E2E — post-deploy Playwright tests against the deployed webapp
  # =========================================================================
  #
  # Runs after deploy completes. Gracefully skips if login secrets
  # are not configured yet. Start with a minimal deploy-gate journey
  # (login → authenticated landing) before expanding to deeper flows.
  #
  # projectName-browser-e2e:
  #   name: projectName-browser-e2e
  #   runs-on: ubuntu-latest
  #   needs: deploy
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   env:
  #     E2E_BASE_URL: https://qa.<your-domain>.com
  #     E2E_EMAIL: ${{ secrets.QA_E2E_EMAIL }}
  #     E2E_PASSWORD: ${{ secrets.QA_E2E_PASSWORD }}
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: 22
  #         cache: npm
  #     - run: npm ci
  #
  #     - name: Detect browser E2E readiness
  #       id: browser-e2e-ready
  #       run: |
  #         if [ -z "$E2E_EMAIL" ] || [ -z "$E2E_PASSWORD" ]; then
  #           echo "ready=false" >> $GITHUB_OUTPUT
  #           echo "Browser E2E secrets not configured — skipping."
  #         else
  #           echo "ready=true" >> $GITHUB_OUTPUT
  #         fi
  #
  #     - name: Install Playwright Chromium
  #       if: steps.browser-e2e-ready.outputs.ready == 'true'
  #       run: cd clients/<projectName> && npx playwright install --with-deps chromium
  #
  #     - name: Run browser E2E
  #       if: steps.browser-e2e-ready.outputs.ready == 'true'
  #       run: cd clients/<projectName> && npx playwright test
  #
  #     - name: Upload browser E2E artifacts
  #       if: always() && steps.browser-e2e-ready.outputs.ready == 'true'
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: projectName-browser-e2e
  #         path: |
  #           clients/<projectName>/playwright-report/**
  #           clients/<projectName>/test-results/**
  #         if-no-files-found: warn
  #
  #     - name: Browser E2E summary
  #       if: always()
  #       run: |
  #         echo "### Browser E2E" >> $GITHUB_STEP_SUMMARY
  #         echo "" >> $GITHUB_STEP_SUMMARY
  #         echo "- Base URL: \`$E2E_BASE_URL\`" >> $GITHUB_STEP_SUMMARY
  #         if [ "${{ steps.browser-e2e-ready.outputs.ready }}" != "true" ]; then
  #           echo "- Status: skipped (secrets not configured)" >> $GITHUB_STEP_SUMMARY
  #         else
  #           echo "- Status: executed" >> $GITHUB_STEP_SUMMARY
  #         fi
